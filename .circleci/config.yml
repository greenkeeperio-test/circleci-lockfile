# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10

    steps:
      - run: npm install
      - run: npx -p greenkeeperio/greenkeeper-lockfile#feat/monorepos greenkeeper-lockfile-update
      - run: npm test
      - run: npx -p greenkeeperio/greenkeeper-lockfile#feat/monorepos greenkeeper-lockfile-upload
